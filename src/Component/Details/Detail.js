import React from "react";
import { Button } from "react-bootstrap";
import { useHistory } from "react-router";

const Detail = (props) => {
  const { name, img, discription, _id } = props.detail;

  return (
    <div>
      <h1>this is deauls</h1>
    </div>
  );
};

export default Detail;
